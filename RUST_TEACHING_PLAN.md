# Rust 入门与工程开发教学实施计划

> 项目：nativeLanguageLearn  
> 主项目：LogLens  
> 技术范围：全周期仅使用 Rust，不设置 C++ 对照路线
> 建议周期：16 周  
> 建议强度：每周 5 次，每次 60～120 分钟  
> 版本：v1.2
> 更新日期：2026-08-27

## 1. 课程定位

本课程面向已有常见应用系统开发经验、能够交付功能模块，希望入门 Rust 并将已有工程能力迁移到 Rust 的学习者。已有应用开发经验不等于已经掌握 Rust 的所有权、类型或并发机制，后者仍需讲解与独立验证。

课程不以记忆语法为目标，而以持续交付一个真实工具为主线。每个知识点必须服务于可观察的项目能力，并通过测试、真实数据、解释和隔日复现完成闭环。

课程完成后，学习者应能：

1. 阅读中等规模的 Rust 工程，定位模块、类型、错误和资源边界。
2. 使用所有权、借用、Trait 和泛型设计清晰且安全的 API。
3. 实现 CLI、并发任务、异步 HTTP 服务、缓存和持久化模块。
4. 使用测试、静态检查、基准和 Profiling 验证正确性与性能。
5. 在领域规则明确的前提下，独立设计、实现、测试并接入一个边界明确的安全 Rust 模块，解释方案并回应代码评审。
6. 在陌生但规模受控的 Rust 工程中定位问题，查阅文档、做最小实验并提交带回归测试的修改，而不依赖教师逐步指路。

课程不负责完整教授编译、网络服务、基础组件、OS 内核或数据库内核等岗位领域知识；实验所需规则由任务提供。目标是 Rust 工程参与能力，不承诺完成课程即可获得录用。术语与已确认的设计边界见 [CONTEXT.md](./CONTEXT.md) 和 [课程决策记录](./docs/adr/0001-rust-course-scope-and-budget.md)。

必修以安全 Rust 为主；unsafe 与 FFI 需认识用途、安全责任及阅读边界，但独立实现底层 unsafe 封装、复杂 FFI 不作为结课门槛。

## 2. 学习前提与入学诊断

### 2.1 建议前提

- 已能使用一种语言完成常见应用模块的设计、实现、测试与交付。
- 理解进程、线程、文件、网络请求等基本概念。
- 会使用终端和 Git 的基本命令。
- 不要求提前掌握 Rust、异步编程或系统性能分析。

### 2.2 入学诊断任务

优先用已有项目说明或交付记录确认通用工程经验，不要求有经验的学习者重新参加编程入门训练。仅在相关经验尚不明确时，使用以下 90 分钟诊断替代首次课的基础练习，不额外叠加课时：用当前最熟悉的语言实现“读取日志、按关键词过滤、统计命中数”。诊断只用于调节难度，不计成绩。

观察以下证据：

- 能否把需求拆成输入、处理、输出和错误路径。
- 是否主动处理空文件、不存在文件和非法参数。
- 是否会写至少一个自动测试。
- 是否能解释数据结构和模块边界。

若无法完成主流程，第 1～2 周每次只安排一个成功路径；若 45 分钟内完成并测试充分，可从第 2 周开始增加挑战任务。

## 3. 教学项目与能力路线

长期主项目为 **LogLens**：一个从本地 CLI 演进为日志查询服务的开发者工具。

```text
读取日志
  → 过滤与统计
  → 配置和错误上下文
  → 可插拔解析与输出
  → 多文件并发扫描
  → 异步 HTTP 查询
  → 索引、缓存和持久化
  → 性能分析、CI 和发布
```

每两周形成一个可演示版本。阶段结束时，旧能力不能被新功能破坏。

本次调整保留现有 16 周主线、每周课次及单次时长，也保留已列出的第 5 周后置四个集合切片、命令行重构专题和环境实验。下文的能力覆盖表、资料练习和阶段考核都是这些既有课次的内容映射或练习替换，不是新增必修课；不得以课后作业名义再追加一套项目。第 4 周环境实验按已有安排实施，设备差异不转化为额外 IDE 的必修要求。

已完成的课程和实现继续保留。新增能力落点不追溯认定学习者“已掌握”或“未通过”；缺少证据的点在下一相关课替换同类复习验证，不要求整周重学。

## 4. 教学环境与设施

### 4.1 基础设备

- 一台可稳定运行 64 位 Linux、macOS 或 Windows 的计算机。
- 建议 8 GB 以上内存、10 GB 可用磁盘；并发和性能阶段建议 16 GB 内存。
- 可访问真实或脱敏日志；没有真实数据时准备 1 MB、100 MB、1 GB 三档样本。
- 一个独立 Git 仓库，保留每次能力切片的提交记录。

### 4.2 软件环境

- Rust stable 工具链与 Cargo。
- `rustfmt`、Clippy 和 Rust Analyzer。
- Git、支持断点调试的编辑器、HTTP 客户端和基础系统监控工具。
- 性能阶段以 Linux 服务端诊断为主线，准备可使用 `perf_event` 的 Linux 主机、虚拟机或远程实验环境；容器只有在明确具备所需权限时才用于采样。
- Linux 准备 `perf`、cargo-flamegraph，以及 DHAT 或 heaptrack；macOS 准备 Instruments，作为相同诊断概念的本地映射与交叉验证。
- 固定记录 CPU、操作系统、内核、Rust 工具链、Profiler 版本、构建 profile 和完整 workload，修改前后尽量使用同一环境。

首次课完成环境验收：

```bash
rustc --version
cargo --version
cargo fmt --check
cargo check
cargo test
cargo clippy -- -D warnings
```

### 4.3 教学资料

每周资料分四类，避免无目的收藏：

1. 一份概念材料：只覆盖本周必需知识。
2. 一段工业源码：建议控制在 50～200 行。
3. 一个独立实验：验证容易误解的语言行为。
4. 一张复盘卡：记录问题、证据、结论和迁移场景。

## 5. 教学原则

### 5.1 能力切片

每次课只交付一个能在 60～120 分钟完成的能力，例如“错误中包含文件路径”，而不是笼统地“学习错误处理”。

切片应让学习者独立完成约 70%。若同时出现三个以上全新核心概念，应继续拆小。

### 5.2 核心机制先教后用，接口逐步自主查证

基础教学、迁移练习和判断题不得偷偷考查未介绍过的 Rust 核心机制。若任务需要新语法、所有权规则、类型机制或并发模型，AI 教师先说明语法形式、最小心智模型、所有权边界和常见失败方式，再要求预测或编码；不得把 IDE 提示或学习者猜测当作讲解的替代品。

第 5 周起，在所需机制已有证据的前提下，可将一个具体 API 或库的使用安排为“独立资料练习”：学习者阅读官方文档与签名、编写最小实验、解释输入输出及失败行为，再用于模块；教师提供纠偏和评审，不直接给实现步骤。若查阅中出现未学的核心机制，先暂停该部分并补讲，不把教师漏讲记为学生失败。首次课与基础课仍使用教师示范；独立练习也不要求脱离文档闭卷开发。

### 5.3 证据驱动

“会了”必须有证据：

| 时间 | 证据 |
| --- | --- |
| 几分钟内 | 编译、格式化或静态检查结果 |
| 当次课 | 正常、边界和失败测试 |
| 当天 | 使用真实日志完成一个任务 |
| 隔日 | 不看答案复现核心类型或函数 |
| 阶段末 | 在新需求中迁移旧知识 |

### 5.4 先约束问题，再增加抽象

先完成最短纵向链路，再让重复、变化或性能证据推动重构。没有真实变化点时，不预设复杂 Trait 层级或异步架构。

### 5.5 AI 教师与学生的责任边界

AI 在全周期承担完整的教学责任，包括组织对话、讲解、追问、反馈、调节难度和主持验收；学习者承担主要的思考、编码、运行、解释和复现责任。下表比例只表示项目实现由谁产出，不表示 AI 减少授课职责。

| 阶段 | 学习者实现占比 | AI 直接生成实现上限 | AI 教师主要职责 |
| --- | ---: | ---: | --- |
| 第 1～4 周 | 60% | 40% | 拆分任务、解释诊断、示范最小且业务不同的模式 |
| 第 5～10 周 | 80% | 20% | 苏格拉底式提问、测试建议、接口审查 |
| 第 11～16 周 | 90% | 10% | 架构质询、性能审查、模拟验收 |

以上比例只用于教学过程观察，不作为独立能力的证明。关键接口、所有权、错误处理或实现步骤若由教师给出，即使学生录入了全部代码，该部分仍属于指导下完成；独立考核时 AI 不生成待考模块的设计或实现。教师可澄清领域规则、处理环境问题并进行正常评审，但若反馈已经给出解法，应记录帮助并另用等价变体验证独立性。

### 5.6 核心能力覆盖与先修关系

表中“认识”要求能说明用途和边界，“运用”要求能独立实现、调试或验证；整项能力还需通过异题迁移才能认定具备迁移能力。以下内容嵌入指定课次，不是额外课程，也不要求穷尽标准库。

| 能力 | 首次与深化落点 | 验收及深度边界 |
| --- | --- | --- |
| 基本类型、模式匹配、`Option` / `Result`、模块可见性 | 第 1～2 周；第 5 周公共 API | 能阅读签名、穷尽处理分支、定位错误和控制公开范围 |
| 所有权、借用、切片、资源释放 | 第 3 周作用域与提前返回；第 5 周学过 Trait 后认识 `Drop`；第 8 周 guard | 能说明值与资源何时释放；不以到处 `clone` 或延长锁持有时间规避设计问题 |
| 生命周期、省略规则、结构体借用、`'static` 约束 | 第 4 周；第 5 周借用接口；第 7、9 周线程与任务 | 能解释引用关系并修复典型错误，不将 `'static` 类型约束误读为值必须永远存活 |
| 泛型、Trait bound、`impl Trait` / `dyn Trait`、`Box` | 第 5 周第 2～4 课；集合专题中的关联类型 | 能选择静态或动态接口、借用或拥有；`Box` 用法先讲后用于拥有型 Trait Object；不为练习强加无需求的抽象 |
| 闭包捕获、`move`、`Fn` / `FnMut` / `FnOnce` | 第 1 周最小闭包；第 6 周第 1 课深化；第 7 周线程迁移 | 能根据捕获值的使用推导调用约束；`move` 不机械等同于“只能调用一次” |
| 集合、迭代协议与惰性处理 | 第 5 周后置专题首次学习；第 6 周迁移；第 11～12 周复用 | 能解释元素类型、遍历后的所有权和物化边界；顺序、哈希及相等性满足契约 |
| 错误类型与转换 | 第 2 周错误分类；第 5 周第 4 课 `Display` / `Error` 与 `From`；第 9 周边界映射 | 能保留来源、区分可恢复错误与程序缺陷；已学 Trait 后再实现标准错误接口 |
| 智能指针、内部可变性与共享 | 第 5 周 `Box`；第 7 周 `Rc` / `Arc`；第 8 周 `RefCell` 与锁的边界 | 运用线程安全的共享方式；认识引用计数、运行时借用检查及循环引用风险，不把 `Arc` 等同于内部值自动线程安全 |
| 并发与异步资源边界 | 第 7～10 周，先闭包与资源模型，后 `Send` / `Sync`、Future 与取消 | 能控制任务、锁和资源生命周期；第 9 周认识 `Pin` / `Unpin`、唤醒模型的用途，不要求手写执行器或 unsafe pinning |
| crate 与依赖边界、文档和常见宏 | 第 1 周 Cargo；第 6 周库入口、derive 与 API 文档；第 9 周依赖 feature；第 15 周审查 | 会使用已有宏、阅读依赖文档及版本/feature 约束；独立编写过程宏、复杂 workspace 不作为必修 |
| unsafe / FFI 安全边界 | 第 15 周依赖审查中的短源码阅读 | 认识调用前提与安全封装的责任；独立实现为选修，不以 unsafe 绕过尚未理解的借用错误 |

教师按 [Rust 官方教材](https://doc.rust-lang.org/book/) 及对应标准库文档核对机制，重点参考资源管理、闭包、并发和异步章节；教材是查证来源，不另加“整本学完”的作业。

## 6. 单次课程标准流程

1. **回忆，5 分钟**：关闭资料，说明上次能力、设计原因和易错点。
2. **定义验收，5～10 分钟**：写一个正常、一个边界、一个失败场景。
3. **新概念预讲或资料实验，5～10 分钟**：核心机制由教师先讲；符合第 5.2 节条件的 API 由学生查证并演示最小实验，不把自动补全视为理解。
4. **自主设计，10 分钟**：写输入输出、类型、边界、失败点和暂不处理项。
5. **最小实现，25～45 分钟**：先跑通一个端到端示例。
6. **自动验证，15～20 分钟**：补测试并运行完整质量门禁。
7. **真实使用，10～20 分钟**：使用真实日志或请求验证价值。
8. **解释复盘，10 分钟**：说明方案、替代方案、风险和仍依赖 AI 的部分。
9. **安排复现，2 分钟**：指定下次课前要脱离 AI 重写的内容。

上述分项时间是参考，不得机械相加突破单次 60～120 分钟。熟悉的通用设计流程从简，验证与真实使用可合并；复杂模块沿既有连续课次推进，每次只验收一个切片。独立考核时保留验收与复盘，不预先提供设计骨架或解法提示。

## 7. 16 周详细教学安排

### 第 1 周：建立最小 CLI 闭环

**周目标**：从命令行读取日志，完成过滤和统计，熟悉 Cargo 的反馈循环。

可直接交给高级模型执行的对话式教学 Prompt 见：[第一周 AI 对话式教学执行 Prompt](./WEEK_01_RUST_TEACHING_PLAN.md)。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 创建工程并读取指定文件 | Cargo、变量、函数、`String`、`Result` | 正常、空文件和文件不存在均有明确行为 |
| 2 | 按关键词过滤 | 迭代、闭包、字符串查找 | 支持有匹配、无匹配、中文和大小写选项 |
| 3 | 建模日志级别 | Struct、Enum、模式匹配 | 支持 INFO、WARN、ERROR 过滤和统计 |
| 4 | 拆分读取、解析、过滤、输出 | 模块、可见性、单元测试 | 每个模块职责可用一句话解释 |
| 5 | 调查一份真实日志 | 命令行参数、端到端运行 | 输出一份真实错误调查报告 |

周末闭卷任务：从空工程重建“读取—过滤—统计”主流程，限时 60 分钟。

### 第 2 周：错误处理与可靠输入

**周目标**：让错误成为 API 的一部分，而不是到处打印或崩溃。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 区分参数、I/O 和解析错误 | `Result`、`Option`、`?` | 三类错误可辨识且不丢失原因 |
| 2 | 为错误补充文件和行号上下文 | 错误传播、来源链 | 错误能定位输入位置 |
| 3 | 逐行读取大文件 | `BufRead`、所有权初步 | 内存不随文件大小等比例增长 |
| 4 | 增加集成测试夹具 | `tests/`、临时输入、进程输出 | CLI 成功码和失败码均有测试 |
| 5 | 发布 v0.1 | 文档、帮助文本、Git 标签概念 | 新用户能按 README 独立运行 |

阶段验收：现场加入一种非法日志格式；学习者须在 45 分钟内修改行为并补测试。

### 第 3 周：所有权、借用与 API

**周目标**：从真实编译错误理解数据所有权，并减少不必要复制。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 标注记录在各模块间的所有者 | Move、Copy、作用域与资源释放 | 一张数据生命周期图，含正常结束和提前返回 |
| 2 | 让过滤器借用记录 | 引用、可变借用 | 主流程不复制完整日志行 |
| 3 | 在输入与结果间选择拥有或借用 | `String`、`&str` | 能解释 API 为什么接收该类型 |
| 4 | 修复三个典型借用冲突 | 借用规则、重借用 | 每个错误均写原因而非只记录修法 |
| 5 | 检查克隆和分配 | `clone` 成本、测量意识 | 删除无依据的复制或说明保留理由 |

### 第 4 周：生命周期与配置边界

**周目标**：只在引用关系需要表达时使用生命周期，并完成多来源配置。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 返回输入中的匹配片段 | 生命周期参数、省略规则、多输入关系 | 能画出返回引用与输入的约束，并解释编译失败的反例 |
| 2 | 组合默认值、配置文件和 CLI | Struct、更新语法、优先级 | 配置覆盖顺序有测试 |
| 3 | 校验配置 | 类型约束、构造函数 | 非法状态不能进入主流程 |
| 实验 | 使用 CodeLLDB 调试配置加载 | 断点、单步、变量与调用栈 | 能在 `main` 与 `config` 的分支观察 `Option<bool>`、`Result` 和错误退出码 |
| 实验 | 统一 IDE 的 Rust 工具链 | `PATH`、Homebrew `opt` 链接、Rustup 代理 | VS Code、CodeLLDB 与 RustRover 显示同一 `cargo` / `rustc` 来源，并能解释选择原因 |
| 4 | 阅读一个工业配置模块 | 源码导航、设计笔记 | 一页模块边界与错误策略笔记 |
| 5 | 发布 v0.2 | 回归测试、迁移说明 | 老命令仍可运行，新配置可演示 |

阶段验收：给出一段含多余生命周期或大量 `clone` 的代码，要求解释并改进。

### 第 5 周：Trait 与可插拔过滤器

**周目标**：让新增过滤策略不修改扫描主流程。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 抽取过滤行为 | Trait、实现块 | 关键词与级别过滤共用主流程 |
| 2 | 组合 AND/OR 过滤 | 泛型、组合 | 组合行为有真值表测试 |
| 3 | 比较静态与动态分派 | Trait bound、Trait Object；`Box` 的拥有与释放 | 比较借用型和拥有型接口；以已学 Trait 认识 `Drop`，不要求改用动态分发 |
| 4 | 设计面向调用者的接口 | API 最小化、借用字段与生命周期；`Display` / `Error`、`From` | 为已有模块补全一个可解释的边界，包括所有权和错误来源；不为过滤器硬加关联类型 |
| 5 | 迁移一个新过滤器 | 开闭变化验证、独立接口设计 | 只给时间范围规则和输入夹具，由学生设计过滤器并保持主流程不变 |

第 3～4 课中的指针、释放与错误接口以最小独立示例分开讲解，不要求同一模块同时引入全部机制；关联类型在后置集合专题用 `Iterator::Item` 首次正式讲解。首次独立资料练习放在第 5 课的一个具体 API 上，遇到未学核心机制仍由教师补讲。

**延后重构专题：命令行解析边界**

在第 5 周过滤器功能稳定后单独安排，不与过滤器能力切片混合提交。

| 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- |
| 将手写 CLI 解析从 `run` 提取为 `CliOptions::parse` | 数据簇、职责边界、`Option`、`ok_or_else`、`map_err`、`?` | 参数解析不执行 I/O；`run` 只加载配置、构造过滤器并调度扫描；CLI 行为与退出码回归测试保持通过 |

### 第 5 周后置专题：Rust 集合与迭代协议

**专题目标**：首次系统建立 Rust 集合模型，能够选择标准集合、解释遍历时的所有权变化，并设计一个符合 Rust 惯例的领域集合。保留原有四个连续能力切片；与第 6 周的关系是“首次学习 → 工程迁移”，不是两次重做相同练习。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 使用顺序集合 | 数组、切片、`Vec<T>`、`VecDeque<T>`、连续布局、长度与容量 | 根据顺序访问和两端操作需求选择集合，并实现增删查与边界测试 |
| 2 | 使用键值、有序与优先级集合 | `HashMap`、`HashSet`、`BTreeMap`、`BTreeSet`、`BinaryHeap`、哈希与顺序语义 | 根据查找、去重、有序遍历和优先级需求选择集合，并用测试证明选择结果 |
| 3 | 理解集合的迭代协议 | `Iterator::Item`、`IntoIterator`、`iter`、`iter_mut`、`into_iter`、借用与所有权 | 能预测三种遍历方式的元素类型和遍历后的可用性，并实现一个最小自定义迭代器 |
| 4 | 设计惯用的领域集合 | newtype、内部不变量、`Default`、`len`、`is_empty`、`iter`、`IntoIterator`、`FromIterator`、`Extend` | 实现 `LogRecords` 集合；支持构造、追加、借用遍历和 `collect`，且内部容器不直接暴露 |

集合选择采用已熟悉的业务规则，重点在 Rust 的借用与拥有、API 和迭代协议，不重教通用数据结构课程。不要求把表中的每一种容器都接入 LogLens；用小实验比较后只采用有实际需求的容器。

### 第 6 周：迭代器与输出管线

**周目标**：建立惰性处理管线，支持多种输出格式。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 用迭代器表达过滤与映射 | 惰性适配器、闭包捕获、`Fn` / `FnMut` / `FnOnce` | 与循环版本行为一致，并解释闭包为何可重复调用或只能调用一次 |
| 2 | 将已有迭代器迁移到记录管线 | 复用专题实现、`next`、错误与结束状态 | 加入坏记录与提前停止，解释为何不需要先 `collect` 全部记录 |
| 3 | 支持文本和结构化输出 | 泛型或 Trait Object、常见 derive；库入口与公共 API | 输出器可替换且无业务泄漏；通过公共入口测试，不依赖私有实现 |
| 4 | 处理写入失败 | I/O 错误、短写概念 | 管道关闭时不误报成功 |
| 5 | 发布 v0.3 | 公共 API 文档 | 两种过滤器与两种输出器可组合 |

阶段验收：实现一个未预告的“只取前 N 条”处理器，并解释放在管线中的位置。教师给行为契约、不指定具体适配器；学习者可查文档，验证提前停止与错误传播。这替代本阶段已有迁移题，不额外增课。

### 第 7 周：线程与任务分解

**周目标**：并行扫描多个文件，先保证结果正确和可重复。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 为每个文件创建工作任务 | Thread、`move` 闭包、任务的 `Send` / `'static` 约束 | 多文件结果与串行版一致；能解释传入任务的数据归谁拥有 |
| 2 | 汇总结果 | Channel、所有权转移 | 发送方退出和接收方结束正确 |
| 3 | 共享只读配置 | `Rc` 与 `Arc` 的边界、`Send` / `Sync` | 能解释克隆引用计数指针与克隆数据的区别，以及编译器阻止的风险 |
| 4 | 处理单任务失败 | Join、部分失败策略 | 一个坏文件不丢失其他成功结果 |
| 5 | 测量串行与并行版本 | 计时、变量控制 | 至少三组文件规模数据 |

### 第 8 周：同步、背压与并发验证

**周目标**：控制资源上限，避免“线程越多越快”的错误假设。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 限制并发任务数量 | 工作队列、背压 | 打开文件数不超过配置上限 |
| 2 | 保护共享指标 | Mutex、Atomic；认识 `RefCell` 的运行时借用边界 | 正确使用锁或简单原子计数并解释选择；不要求实现无锁算法 |
| 3 | 避免锁范围过大 | guard 与资源释放、临界区、锁竞争 | 改动前后有测量数据，提前返回时锁释放有验证 |
| 4 | 做并发故障注入 | 超时、提前退出、压力输入 | 无死锁、无永久等待 |
| 5 | 发布 v0.4 | 可重复基准、设计文档 | 并发策略和限制条件写入文档 |

阶段验收：改变 CPU 数和文件大小，要求解释性能曲线而非只报告“更快”。

第 8 周第 4～5 课在既有故障验证与发布任务内完成一次“陌生模块修复”：教师提供小型、可运行且依赖已学机制的扫描模块和故障描述，学生自行定位、写回归测试、提交修复说明并回应一次评审。不要求大型仓库搭建或等待外部维护者合并；它替代同类故障练习，不另起完整项目。

### 第 9 周：异步基础与 HTTP 边界

**周目标**：把查询能力暴露为最小 HTTP 服务，并理解 Future 的惰性与轮询模型。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 用状态机解释异步任务 | Future、`.await`、轮询与唤醒；认识 `Pin` / `Unpin` 用途 | 能手绘暂停、恢复和跨 await 保存的数据；不要求手写执行器 |
| 2 | 实现健康检查 | 异步运行时、Handler、依赖版本与 feature | 在教师讲清运行模型后查阅库文档；服务启动和健康检查有集成验证 |
| 3 | 实现日志查询接口 | 请求/响应、共享状态、任务的拥有和生命周期 | 参数校验和错误映射明确；接口语义由夹具给定，不转成网络协议课程 |
| 4 | 隔离阻塞文件 I/O | 阻塞边界、任务调度 | 并发请求下不会阻塞整个执行器 |
| 5 | 使用真实客户端查询 | HTTP 调试、日志 | 保存一次完整请求证据 |

### 第 10 周：取消、超时与优雅退出

**周目标**：正确管理异步任务生命周期。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 为查询增加超时 | 超时组合、错误分类 | 超时与业务错误可区分 |
| 2 | 传播取消 | 任务收束、Future 被丢弃与取消安全 | 区分请求 Future、已生成的任务和阻塞工作；客户端放弃后的停止与清理有验证 |
| 3 | 限制并发请求 | 信号量、背压 | 超载时返回明确行为 |
| 4 | 实现优雅退出 | 信号、任务收束 | 退出时不接受新请求并等待在途请求 |
| 5 | 发布 v0.5 | 故障测试、运行手册 | 启动、停止和故障排查可复现 |

阶段验收：在慢查询、客户端断连和进程退出同时发生时，说明每个资源的最终状态。

阶段验收不提前给出修法；只提供可复现时序和预期行为，允许查运行时文档。通用 HTTP 背景和故障条件由教师提供，重点考查 Rust 任务与资源边界。

### 第 11 周：索引设计

**周目标**：用查询特征驱动索引，而不是盲目增加数据结构。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 建立查询基线 | 延迟分位数、数据集 | 固定样本和基线报告 |
| 2 | 按级别建立内存索引 | HashMap、复杂度 | 索引与全扫结果一致 |
| 3 | 处理增量数据 | 更新策略、一致性 | 新增日志可被查询且不重复 |
| 4 | 估算内存成本 | 容量、分配、数据布局 | 每百万条记录的内存估算 |
| 5 | 判断索引适用边界 | 时间/空间权衡 | 给出启用与禁用条件 |

本周的索引规则保持简单且明确；第 1～2 课由学生自行提出接口、数据所有权和测试方案。教师评审契约，不提供完整类型骨架，为第 12 周独立模块练习准备。

### 第 12 周：缓存与可观测性

**周目标**：实现有边界的查询缓存，并证明它确实有价值。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 定义缓存键和值 | Hash、等价性 | 不同查询不会错误复用结果 |
| 2 | 实现容量限制与淘汰 | LRU 思想、集合组合 | 命中、淘汰和更新有测试 |
| 3 | 处理缓存失效 | 一致性、时间边界 | 文件变化后不返回过期结果 |
| 4 | 增加指标 | 命中率、延迟、容量 | 可回答缓存是否有效 |
| 5 | 压测冷热查询 | 工作负载设计 | 冷热数据报告与结论 |

本周五课共同构成一次独立模块开发练习，不追加作业项目：教师给定缓存命中、淘汰、失效和容量规则，学生负责 Rust 类型、公开接口、错误/资源边界、实现、测试与接入。算法背景可先解释，但不得直接给 Rust 解法；一次正常评审后由学生修订并说明取舍。若关键方案由教师提供，记录为指导下完成，后续用变体验证，不以录入代码比例替代证据。

### 第 13 周：持久化与恢复

**周目标**：让索引跨进程重启恢复，并明确数据损坏策略。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 明确保存与加载契约 | 序列化库的使用、版本字段 | 使用给定的简单格式规则，说明 Rust 类型与格式间的映射及兼容性约定 |
| 2 | 保存与加载索引 | 文件 I/O、原子替换 | 中断写入不破坏旧数据 |
| 3 | 检测损坏和不兼容 | 校验、错误策略 | 损坏样本有明确恢复路径 |
| 4 | 对比重建与加载成本 | 时间/空间测量 | 确定何时值得持久化 |
| 5 | 发布 v0.6 | 恢复演练 | 删除、损坏、升级三种场景可演示 |

阶段验收：随机终止保存过程，重启后服务必须恢复到明确且可解释的状态。

### 第 14 周：Benchmark 与 Profiling

**周目标**：以 Linux 服务端为主要环境，建立“定义症状—测量—分类—归因—修改—复测”的性能优化流程；能够把同一诊断模型映射到 macOS Instruments。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 建立稳定基线 | release/bench profile、Criterion、端到端 workload、预热、噪声、样本 | 三档数据规模的耗时、吞吐、方差或置信区间 |
| 2 | 在 Linux 初步分类瓶颈 | `perf stat`、wall/CPU time、context switches、page faults；条件允许时比较 instructions、cycles、IPC | 提出 CPU 或等待等优先调查方向，不能凭计数器直接确定分配、锁或 I/O 根因 |
| 3 | 在 Linux 归属 CPU 开销 | `perf record/report`、cargo-flamegraph、采样、调用栈、self 与 inclusive cost | 标出前三个热点、进入路径、占比和一个可证伪假设 |
| 4 | 检查分配并验证假设 | DHAT 或 heaptrack 选一个、分配次数、累计字节、峰值存活量、分配点 | 实施一项有证据的借用、复用或循环外提优化 |
| 5 | 跨环境复测并写结论 | Linux 复测、macOS Instruments 映射、统计变化、行为回归、适用边界 | 优化前后报告、行为等价证据、跨环境差异与副作用 |

五课围绕同一个瓶颈案例推进，不逐一巡游所有工具。Linux 采样与分配诊断是主要实操；macOS 在既有本地环境映射同一组概念，不用不同硬件的绝对耗时证明改动收益。Cachegrind/Callgrind、深入缓存/分支事件分析、汇编和编译参数调优为进阶选修，不额外挤入必修验收。

#### 第 14 周诊断路径

性能诊断必须从用户可观察的症状开始，不能先打开火焰图再寻找故事：

```text
固定 workload 和正确性基线
  → 测量 wall time、吞吐、延迟与波动
  → 用 perf stat 为 CPU、调度/等待、缺页等方向建立初步线索
  → CPU 路径用 perf record/report 或 cargo-flamegraph 归属到调用栈
  → malloc/free、memcpy 或容器扩容显著时检查分配
  → 为执行次数、分配次数或数据规模建立成本模型
  → 提出一个可证伪假设，只修改一个主要因素
  → 微基准、端到端基准和行为测试共同复测
```

指标解释要求：

- wall time 代表用户等待时间；CPU time 和 CPU 利用率用于判断程序是在计算还是等待，二者不能互相替代。
- `instructions`、`cycles` 和 IPC 用于比较执行工作量与 CPU 效率；cache miss、branch miss 只作为进一步调查线索，不能单独构成根因。
- CPU Profile 同时查看函数自身消耗和包含子调用的总消耗，并沿调用栈确认热点由哪条业务路径触发。
- 火焰图宽度表示所选采样事件的聚合权重，不是调用次数或时间先后；CPU 采样不能单独解释阻塞等待。需要等待归因时，应结合线程状态、时间线或有针对性的等待观测，并明确尚未证实的假设。
- 分配诊断同时查看分配次数、累计分配字节、峰值存活字节和分配调用点；峰值内存低不代表分配开销低。
- wall time 很高而 CPU 不忙时，应检查阻塞、锁、调度和系统调用等待，不能继续做无证据的 CPU 微优化。
- 多核总利用率低不排除单线程 CPU 热点；虚拟机中硬件计数器不可用时，记录限制并使用可用的采样/时间证据，不将缺失指标填成零，也不把不同环境的差异算成优化收益。

Linux 为本周主要验收环境。macOS 使用 Instruments Time Profiler 和 Allocations 复现 self/total cost、调用栈与分配点等相同概念，但不能代替 Linux 上的 `perf` 诊断证据。采样 release 构建时保留行号级调试信息；调用栈缺失时再验证 frame pointer、符号和内核 `perf_event` 权限，不以永久放宽系统安全策略作为课程默认方案。

### 第 15 周：工程化与发布准备

**周目标**：让项目可由他人构建、测试、配置和排障。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 固化质量门禁 | fmt、Clippy、test | 本地一条命令完成检查 |
| 2 | 配置 CI | 构建矩阵、缓存意识 | 新提交自动验证 |
| 3 | 完善文档 | API、示例、故障排查 | 新用户从零运行成功 |
| 4 | 审核依赖与许可证 | 版本与 feature、供应链意识；认识 unsafe / FFI 边界 | 依赖用途清单；能解释一个安全封装或 unsafe 调用的责任，不要求编写底层实现 |
| 5 | 候选版本演练 | 版本、变更日志 | 可安装的候选版本 |

### 第 16 周：综合交付与独立考试

**周目标**：在不提供 Rust 设计与实现步骤的条件下，独立交付一个完整模块，并验证它能在不同调用环境下工作。

| 课次 | 能力切片 | 验收产物 |
| --- | --- | --- |
| 1 | 澄清需求并独立设计模块 | 输入输出、公开接口、所有权、错误与资源边界、验收样例和风险说明 |
| 2 | 独立完成最短纵向链路 | 自行实现的模块、正常路径测试和工程接入 |
| 3 | 补齐边界、故障与回归测试 | 通过公开接口验证行为，保持已有调用者兼容 |
| 4 | 使用真实数据、迁移并做性能复核 | 接入教师提供的小型陌生调用方/测试夹具，解释迁移与性能结果 |
| 5 | 评审修订、答辩并交付 | 回应评审的变更、完整报告；通过门禁后发布 v1.0 |

任务在本周五课内完成：教师提供明确的业务规则、规模上限和可运行夹具，不要求学习额外领域知识或另建完整系统，不预设 Rust 类型、函数签名或待填实现。陌生调用方只用于检验模块的接口与迁移能力，不是第二个开发项目。

允许查官方文档、标准库源码、用调试器和正常代码评审；AI 可解释文档或澄清需求，不生成待考模块的关键设计、实现和测试方案。出现实质解法提示时该部分不能计为独立完成；按第 9.3 节处理，不因到了第 16 周就自动判定结课。

## 8. 作业、复习与源码阅读

### 8.1 每日作业

- 提交一个小而完整的 Git commit。
- 写 100～200 字学习记录。
- 标记一个仍不确定的问题。
- 安排一个 5～10 分钟的隔日复现任务。

功能型提交消息示例：

```text
feat: 支持按日志级别过滤记录
```

### 8.2 每周复习

- 无 AI 重写核心 30～50 行或一个关键类型。
- 用本周知识完成一个业务不同的迁移题；第 8、12、16 周以对应工程任务替代，不另加同类题。
- 阅读一个工业模块，记录“入口、核心状态、错误边界、测试策略”；中后期以第 8 或第 16 周的既有修复/接入任务完成实践，不只写阅读笔记，也不再追加同类任务。
- 删除一条已被证据推翻的旧认知。

### 8.3 间隔复习

- 1 天后：复现核心 API。
- 1 周后：完成变体题。
- 4 周后：把知识用于新模块。
- 8 周后：在综合需求中再次解释设计权衡。

## 9. 评价与阶段验收

### 9.1 评分维度

| 维度 | 权重 | 优秀证据 |
| --- | ---: | --- |
| 功能正确性 | 25% | 正常、边界和失败场景均通过 |
| Rust 类型与资源设计 | 20% | 所有权清晰，API 不依赖无意义复制 |
| 测试与验证 | 20% | 测试覆盖行为，能复现故障 |
| 设计解释 | 15% | 能比较替代方案和成本 |
| 工程质量 | 10% | 格式、静态检查、文档和提交清晰 |
| 独立完成度 | 10% | 可在低 AI 参与下迁移知识 |

总分低于 70 不进入下一阶段；资源安全、数据丢失或并发死锁等关键项不合格时，即使总分达标也必须补修。

评分只用于反馈强弱项，不能抵消独立能力门槛。第 8、12 周按任务记录“指导下完成／独立完成／已证明具备迁移能力”等状态；第 16 周必须独立完成模块并通过迁移验证。高分、代码行数、工具运行次数或 AI 代码占比均不能替代这些证据。

### 9.2 每两周验收

1. 演示一个真实可用的新版本。
2. 展示自动化测试与真实使用证据。
3. 解释三个关键设计决策。
4. 完成一个未见过的迁移任务。
5. 列出仍依赖 AI 教师的能力。

### 9.3 独立模块开发的硬性验收

- **设计**：能依据需求自行选择接口、数据所有权、错误和资源边界，并解释至少一个未采用方案。
- **实现与集成**：能在现有工程约束下交付模块，不依赖教师提供代码骨架或逐步解法。
- **验证与诊断**：正常、边界、失败和兼容性有测试；能用编译器、调试器或最小实验独立定位一个问题。
- **协作**：提供可评审的变更说明，区分需求问题和实现问题，能解释并修订评审指出的问题。
- **迁移**：在业务表面或调用方不同、语言机制相同的任务中复用能力，不只复现 LogLens 原题。

以上项目均需留下日期、任务来源、代码/测试/解释证据和帮助程度。任一项未满足，不判定“独立模块开发能力已达标”；需要教师给出解法的部分回到指导练习，在后续既有练习槽位用变体复测。若现有课时结束仍未达标，应如实交付能力缺口清单，与学习者另行确认后续安排，不擅自延长课程或降低门槛。

## 10. AI 对话式教学协议

AI 教师每个聊天回合只推进一个最小教学动作。需要学习者思考、编码、运行或解释时必须停止并等待证据，不得一次性讲完整课或直接交付项目答案。新核心机制必须先说明用途、语法骨架、关键符号和当前代码映射；新 API 是否采用独立资料练习遵循第 5.2 节，不机械要求每个接口都由教师先讲。IDE 自动补全不算理解，使用后仍须以解释和实验验证。

帮助按六级逐步升级：

1. **复述现状**：让学习者说明目标、现象和最近一次尝试。
2. **指出类别**：指出问题属于参数、类型、控制流、所有权、I/O、模块或测试。
3. **给下一步**：给伪代码或单个动作，不给完整 Rust 实现。
4. **最小独立示例**：给不超过 15 行、与业务不同的示例。
5. **精确 Review**：指出学习者实现中需要修改的位置和原因。
6. **最小参考实现**：只在明确请求或多次失败后提供，并要求解释与变体复现。

模型能访问工作区时可以读取代码和运行安全检查，但默认不替学习者编辑练习实现；纯 Chat 模型必须请学习者粘贴源码、命令和输出，不能假装访问过环境。只有在独立尝试 10～15 分钟并记录失败证据后，才能升级帮助等级。不得直接粘贴无法逐行解释的 AI 代码。

上述等待只适用于已经讲过机制的练习困难，不用于拖延新机制预讲或环境问题排除。每次任务开始先说明是指导练习、独立资料练习还是独立考核，结束按实际帮助记录证据，不把练习过程的成功直接当作独立考核通过。

## 11. 完成定义

每次能力切片完成时必须满足：

- [ ] 程序可编译、可运行。
- [ ] 至少一个正常测试和一个边界或失败测试。
- [ ] `cargo fmt --check`、`cargo check`、`cargo test`、`cargo clippy -- -D warnings` 通过。
- [ ] 使用真实输入完成一次运行。
- [ ] 能解释一个关键设计决策和一个替代方案。
- [ ] 记录一个不足或后续改进点。
- [ ] 形成一个小而完整的 Git commit。
- [ ] 安排下一次无 AI 复现任务。

完成能力切片不等于已具备独立开发或迁移能力；后者还需满足第 9.3 节。状态只随实际证据更新，修改教学计划不能回填掌握记录。

## 12. 动态调整规则

### 任务过难

若基础练习 30 分钟仍无可运行结果、学习者只能完成不足 50%，或同时遇到三个以上新概念，则缩小切片。模块任务应先检查是否已有设计、复现或实验进展，不能仅因 30 分钟未交付完整模块就代写；先区分 Rust 机制缺口与未知领域背景。

### 任务过易

若无需设计即可完成，则增加错误路径、数据规模、资源上限、并发条件或需求变化，不以增加代码行数代替难度。

### AI 依赖过高

连续两周仍由 Codex 生成主要实现时，暂停新增功能，缩小切片，增加闭卷复现，并把 Codex 限制在提问和 Review。

### 进度落后

优先保留错误处理、所有权、测试、并发正确性和综合交付。可压缩输出格式、持久化格式扩展或高级性能挑战，但不跳过阶段验收。

不新增必修周次，不把压缩掉的内容隐性转移到课后。已学点用短提取题确认，真正欠缺的点替换下一相关课的重复练习；总课时内无法达到目标时按第 9.3 节报告，而不是无证据宣告完成。

## 13. 学习记录模板

```markdown
# 学习记录：<能力名称>

日期：
用时：
任务模式：指导练习 / 独立资料练习 / 独立考核
帮助程度：无解法提示 / 第 10 节帮助等级 1～6（注明最高等级）
能力状态：未介绍 / 已介绍 / 指导下完成 / 独立完成 / 已证明具备迁移能力 / 需要复习

## 本次目标与验收示例

## 验收证据
- 编译与静态检查：
- 自动测试：
- 真实使用：
- 证据来源（代码、测试或解释记录）：
- 查阅资料与最小验证实验：
- 关键设计由谁提出，评审后由谁修订：
- 迁移任务与结果（未做须注明）：

## 我能独立解释的内容

## 一个替代设计及其成本

## 当前实现的不足

## 下次无 AI 复现任务
```

## 14. 结课交付清单

- LogLens v1.0 源码和清晰的提交历史。
- 可重复运行的单元、集成、故障和性能测试。
- 安装、配置、使用、排障和架构文档。
- 一份性能基线与至少一次证据驱动的优化报告。
- 一份 Rust 所有权、并发安全和错误边界的设计说明。
- 一次符合第 9.3 节的独立模块交付、陌生调用方迁移、评审修订与答辩。
- 一份核心能力覆盖与证据清单，明确仍需指导的部分，不用项目版本号代替能力结论。
