# C++ 系统编程教学实施计划

> 项目：nativeLanguageLearn  
> 主项目：LogLens C++ Edition  
> 建议周期：16 周  
> 建议强度：每周 5 次，每次 60～120 分钟  
> 推荐标准：C++20 起步，按环境评估 C++23 特性  
> 版本：v1.0  
> 更新日期：2026-07-16

## 1. 课程定位

本课程面向已有编程经验，希望系统掌握 Modern C++、资源管理、泛型、并发和性能工程的学习者。

建议先完成 Rust 路线，再用 C++ 重做关键模块。这样可以对照两种语言如何表达所有权、资源生命周期、并发安全和抽象成本。课程也可独立执行，但需增加指针与内存基础预习。

课程完成后，学习者应能：

1. 阅读和维护使用 Modern C++ 的中等规模工程。
2. 使用 RAII、值语义和 Rule of Zero 设计安全的资源边界。
3. 用模板、Concept 和多态构建受约束、可演进的接口。
4. 实现并验证线程池、缓存、异步服务和自定义内存策略。
5. 使用测试、Sanitizer、Benchmark 和 Profiling 排查正确性与性能问题。
6. 识别悬空引用、未定义行为、数据竞争和异常安全风险。

## 2. 学习前提与入学诊断

### 2.1 建议前提

- 熟悉函数、类、集合、文件 I/O 和基本并发概念。
- 会使用终端、Git 和任意一种构建工具。
- 若未学过 Rust，应先理解栈、堆、指针、引用和对象生命周期。
- 不要求提前掌握模板元编程、协程或自定义分配器。

### 2.2 入学诊断任务

在 90 分钟内实现“读取日志、按关键词过滤、统计命中数”。允许使用熟悉的 C++ 标准，但必须能够构建、运行和测试。

重点观察：

- 是否明确对象所有者和引用有效期。
- 是否手动管理了本可由标准容器或智能指针管理的资源。
- 是否处理文件打开、解析失败和空输入。
- 是否能说明复制、移动和异常路径上的资源状态。

若出现裸 `new/delete`、悬空引用或无法解释的强制转换，第 1～4 周不提前进入并发内容。

## 3. 教学项目与能力路线

主项目为 **LogLens C++ Edition**，与 Rust 版保持相同的用户能力，但不要求逐行翻译。

```text
Modern C++ CLI
  → RAII 资源封装
  → 值语义与数据管线
  → 泛型过滤与序列化
  → 内存资源与缓存
  → 线程池和并行扫描
  → Coroutine HTTP 服务
  → Sanitizer、Profiling 和发布
```

每个阶段都要记录与 Rust 的对照，但结论必须基于代码和测量，不能停留在“哪门语言更安全或更快”的笼统判断。

## 4. 教学环境与设施

### 4.1 基础设备

- 64 位 Linux、macOS 或 Windows 计算机。
- 建议 16 GB 内存和 15 GB 可用磁盘，以便执行多构建配置与 Sanitizer。
- 准备 1 MB、100 MB、1 GB 三档日志，以及包含坏行、超长行和混合编码的故障样本。
- 独立 Git 仓库或与 Rust 版分目录管理，禁止共用构建产物。

### 4.2 编译与构建环境

- 至少一种主编译器；条件允许时使用两种编译器交叉验证。
- CMake、构建执行器、代码格式化工具和静态分析工具。
- 单元测试框架、Benchmark 工具和 HTTP 客户端。
- AddressSanitizer、UndefinedBehaviorSanitizer；并发阶段增加 ThreadSanitizer。
- 调试器和平台 Profiling 工具。

第一课建立以下构建配置：

| 配置 | 用途 | 关键要求 |
| --- | --- | --- |
| Debug | 日常开发 | 调试信息完整，警告视为错误 |
| ASan/UBSan | 内存与 UB 检查 | 测试套件可完整运行 |
| TSan | 并发阶段 | 与其他 Sanitizer 分开运行 |
| Release | 性能与发布 | 固定优化选项并记录环境 |

基础质量门禁：

```bash
cmake -S . -B build
cmake --build build
ctest --test-dir build --output-on-failure
```

### 4.3 教学资料

每周准备：

1. 一份语言概念材料。
2. 一段 50～200 行工业源码。
3. 一个独立的对象生命周期或并发实验。
4. 一份编译器诊断、Sanitizer 报告或性能 Profile。
5. 一张 Rust/C++ 对照记录表。

## 5. 教学原则

### 5.1 资源路径必须可解释

每个文件、Socket、锁、线程、缓冲区和协程状态都要回答：谁拥有、何时释放、失败时如何收束、是否可以移动、是否允许共享。

### 5.2 优先值语义与标准设施

默认使用对象、标准容器和 Rule of Zero。只有在所有权确实需要共享或转移时才引入智能指针；只有标准设施不能满足已测量需求时才引入自定义内存管理。

### 5.3 未定义行为按最高优先级处理

“看起来能运行”不算正确。编译警告、Sanitizer 失败、数据竞争、悬空引用和越界访问必须在增加功能前清零。

### 5.4 抽象必须验证成本

模板、虚函数、类型擦除、分配器和协程都要说明使用理由。性能结论必须来自基准与 Profile，不能只根据语法猜测。

### 5.5 AI 参与度递减

| 阶段 | 学习者投入 | Codex 投入 | Codex 主要职责 |
| --- | ---: | ---: | --- |
| 第 1～4 周 | 65% | 35% | 解释诊断、检查资源边界、辅助拆分 |
| 第 5～10 周 | 80% | 20% | 接口质询、测试与 Sanitizer 建议 |
| 第 11～16 周 | 90% | 10% | 并发审查、性能分析、验收答辩 |

## 6. 单次课程标准流程

1. **回忆，5 分钟**：画出上次新增对象或资源的生命周期。
2. **定义验收，5～10 分钟**：写正常、边界、失败和资源释放场景。
3. **自主设计，10 分钟**：标注所有者、借用者、线程边界和异常保证。
4. **最小实现，25～45 分钟**：先完成一个可运行纵向链路。
5. **自动验证，15～25 分钟**：测试、警告和对应 Sanitizer 全部通过。
6. **真实使用，10～20 分钟**：使用真实日志、并发负载或网络请求。
7. **解释复盘，10 分钟**：比较替代设计、资源风险与抽象成本。
8. **安排复现，2 分钟**：指定下次脱离 AI 重写的类型或算法。

## 7. 16 周详细教学安排

### 第 1 周：Modern C++ CLI 与构建闭环

**周目标**：建立可重复构建和测试的日志读取工具。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 创建工程和多配置构建 | CMake target、编译选项 | Debug 与 Release 均可构建 |
| 2 | 读取并统计日志 | `std::string`、流、错误状态 | 正常、空文件和不存在文件行为明确 |
| 3 | 按关键词过滤 | 算法、lambda、`string_view` | 有匹配、无匹配和中文样本通过 |
| 4 | 日志级别建模 | `enum class`、Struct、`optional` | 非法级别不会进入业务流程 |
| 5 | 模块化并调查真实日志 | 头文件、源文件、测试 target | 输出真实错误调查报告 |

周末闭卷任务：从空目录重建 CMake、读取、过滤和一个测试。

### 第 2 周：容器、算法与接口边界

**周目标**：用标准设施表达数据处理，避免不必要的手写循环和所有权泄漏。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 统计各级别数量 | 容器、算法、迭代器 | 结果与固定样本一致 |
| 2 | 接收连续数据视图 | `span`、借用语义 | API 不复制且生命周期约束被记录 |
| 3 | 区分拥有字符串和字符串视图 | `string`、`string_view` | 无悬空视图，测试覆盖临时对象风险 |
| 4 | 设计解析结果 | `optional`、`variant` 或结果类型 | 成功与失败信息均不丢失 |
| 5 | 发布 v0.1 | 安装、帮助、回归测试 | 新用户按 README 可独立运行 |

阶段验收：现场增加一种坏行策略，并解释为何不会产生悬空引用。

### 第 3 周：RAII 与文件资源

**周目标**：让资源释放由对象生命周期保证。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 追踪文件和缓冲区生命周期 | 栈对象、析构顺序 | 一张成功与失败路径图 |
| 2 | 封装一个系统资源 | RAII、构造/析构 | 所有返回路径均自动释放 |
| 3 | 明确资源所有权 | `unique_ptr`、观察指针 | 禁止不必要共享所有权 |
| 4 | 审查异常路径 | 异常安全、清理 | 注入异常后无资源泄漏 |
| 5 | 运行 ASan/UBSan | 动态诊断 | 测试套件零报告 |

### 第 4 周：Rule of Zero/Five 与异常安全

**周目标**：正确设计可复制、可移动或不可复制的资源类型。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 判定类型语义 | Rule of Zero、复制/移动 | 每个核心类型有语义说明 |
| 2 | 为独占资源实现移动 | 移动构造、移动赋值 | 移动后对象保持有效可析构状态 |
| 3 | 比较三种异常保证 | 基本、强、不抛出 | 一个写入操作标明保证等级 |
| 4 | 用交换或临时对象保护状态 | 提交式更新 | 故障注入不留下半更新状态 |
| 5 | 发布 v0.2 | Sanitizer、设计文档 | 资源边界通过现场答辩 |

阶段验收：修复一段包含双重释放、泄漏和移动后误用的代码，并解释每个根因。

### 第 5 周：值语义、复制与移动成本

**周目标**：建立批量日志记录管线，并用测量决定复制策略。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 批量读取记录 | `vector`、容量、失效规则 | 空、小、大批次均正确 |
| 2 | 检查复制与移动发生位置 | 值类别、移动语义 | 能解释关键调用的对象变化 |
| 3 | 预留容量并复测 | `reserve`、重分配 | 优化前后分配证据 |
| 4 | 设计返回值接口 | 返回值优化、按值传递 | 不用猜测式 `std::move` |
| 5 | 对比对象和指针容器 | 局部性、间接访问 | 给出选择依据和适用边界 |

### 第 6 周：迭代器、Range 与数据管线

**周目标**：让读取、解析、过滤和输出可以组合且保持生命周期安全。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 用算法表达过滤 | 迭代器、谓词 | 与循环版本行为一致 |
| 2 | 构建惰性处理管线 | Range/View 概念 | 不物化不必要的中间容器 |
| 3 | 识别迭代器失效 | 容器修改规则 | 失效场景有最小实验 |
| 4 | 实现批量输出 | 输出迭代或 Writer 接口 | 写入失败可传播 |
| 5 | 发布 v0.3 | API 文档、回归测试 | 管线边界和借用关系可解释 |

阶段验收：增加“取前 N 条”与“跳过前 N 条”，并验证不同组合顺序的结果。

### 第 7 周：模板与 Concept

**周目标**：构建编译期受约束的过滤和序列化接口。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 泛化过滤算法 | 函数模板、推导 | 两种记录类型复用同一算法 |
| 2 | 约束过滤器能力 | Concept、requires | 错误类型产生可理解诊断 |
| 3 | 使用编译期分支 | `if constexpr` | 不同能力选择正确实现 |
| 4 | 控制模板暴露面 | 实例化、编译依赖 | 公共头文件保持最小 |
| 5 | 阅读一个工业泛型接口 | 源码分析 | 记录约束、扩展点与错误体验 |

### 第 8 周：静态、动态多态与类型擦除

**周目标**：根据扩展边界选择多态方式，而不是默认使用继承。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 实现静态过滤策略 | 模板组合 | 编译期组合可测试 |
| 2 | 实现运行时可配置策略 | 虚函数、接口类 | 可从配置选择过滤器 |
| 3 | 尝试类型擦除边界 | 可调用对象、封装 | 调用者不依赖具体类型 |
| 4 | 比较二进制、编译与运行成本 | 测量、权衡 | 一份多态选择矩阵 |
| 5 | 发布 v0.4 | 扩展性演示 | 新增过滤器不修改扫描核心 |

阶段验收：面对“插件在运行时加载”和“性能敏感内循环”两个需求，分别选择并论证接口。

### 第 9 周：内存布局与缓存局部性

**周目标**：理解数据布局如何影响扫描性能。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 检查核心对象大小 | 对齐、填充、`sizeof` | 结构布局图 |
| 2 | 比较 AoS 与 SoA | 数据访问模式、局部性 | 固定负载的基准结果 |
| 3 | 减少临时分配 | 缓冲复用、容量策略 | 分配次数和延迟变化 |
| 4 | 观察分支与访问模式 | 热路径、可预测性 | Profile 支持结论 |
| 5 | 写数据布局决策 | 性能与可维护性 | 明确何时不值得优化 |

### 第 10 周：Allocator、PMR 与缓存

**周目标**：在有证据的场景中应用批量分配和有界缓存。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 统计默认分配行为 | Allocator 概念 | 基线分配报告 |
| 2 | 用单调资源处理批次 | PMR、资源生命周期 | 释放时机明确且无悬空容器 |
| 3 | 比较池化与默认分配 | 工作负载、碎片 | 适用与反例数据 |
| 4 | 实现有容量的 LRU | 哈希表、链表、失效 | 命中、淘汰和更新有测试 |
| 5 | 发布 v0.5 | Benchmark、ASan/UBSan | 自定义内存策略不破坏正确性 |

阶段验收：切换不同批次规模，解释 PMR 何时改善、何时无效或变差。

### 第 11 周：线程、Mutex 与条件变量

**周目标**：实现有界线程池和任务队列。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 启动并收束工作线程 | `thread`/`jthread`、join | 析构时无遗留线程 |
| 2 | 实现受保护任务队列 | Mutex、锁守卫 | 共享状态访问有明确不变量 |
| 3 | 等待任务到达 | Condition Variable、谓词 | 处理虚假唤醒 |
| 4 | 限制队列容量 | 背压、生产消费 | 满队列行为有测试 |
| 5 | 并行扫描多个文件 | 任务拆分、结果汇总 | 与串行结果一致 |

### 第 12 周：Atomic、内存序与并发性能

**周目标**：使用 TSan 验证并发正确性，并谨慎使用原子操作。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 运行并解释 TSan 报告 | 数据竞争、happens-before | 修复一个故意注入的竞争 |
| 2 | 为简单指标选择 Atomic | 原子读改写 | 能解释为何不需要复合不变量 |
| 3 | 理解默认与弱内存序 | 顺序一致、获取/释放概念 | 通过最小实验解释约束 |
| 4 | 检查锁竞争与伪共享 | Cache line、False Sharing | 优化前后有测量 |
| 5 | 发布 v0.6 | 压力测试、TSan | 线程池关闭、取消和异常场景通过 |

阶段验收：随机延迟、任务抛错和提前停止同时发生，程序不得死锁或丢失未定义的任务状态。

### 第 13 周：Coroutine 与异步生命周期

**周目标**：理解协程对象、暂停点和状态所有权。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 拆解最小协程 | Promise、Handle、暂停 | 协程状态生命周期图 |
| 2 | 实现可等待操作 | Awaiter 协议 | 正常与异常恢复路径有测试 |
| 3 | 管理协程返回值和异常 | 结果传播、销毁 | 不泄漏协程帧 |
| 4 | 识别引用跨暂停点风险 | 生命周期、线程切换 | 悬空引用实验被 Sanitizer 捕获 |
| 5 | 建立最小异步查询 | 调度边界 | 单请求端到端运行 |

### 第 14 周：HTTP 服务、取消与关闭

**周目标**：提供可查询真实日志的服务，并正确收束异步资源。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 实现健康检查和查询路由 | 请求/响应、错误映射 | 参数和状态码有集成测试 |
| 2 | 接入扫描和缓存 | 共享状态边界 | 并发查询结果正确 |
| 3 | 增加超时与取消 | 取消传播 | 客户端放弃后工作可收束 |
| 4 | 实现优雅关闭 | 停止令牌、任务生命周期 | 不接受新请求并等待在途任务 |
| 5 | 发布 v0.7 | 运行手册、故障测试 | 启动、停止和恢复可演示 |

阶段验收：解释一个请求从创建到完成、超时或取消时，每个对象由谁销毁。

### 第 15 周：Benchmark、Profiling 与工程化

**周目标**：建立稳定的性能基线和发布门禁。

| 课次 | 能力切片 | 核心知识 | 验收产物 |
| --- | --- | --- | --- |
| 1 | 建立微基准 | 优化干扰、预热、噪声 | 可重复的解析基准 |
| 2 | 建立端到端基准 | 吞吐、延迟、数据规模 | 三档数据报告 |
| 3 | CPU 与内存 Profiling | 热点、分配、调用栈 | 前三热点及解释 |
| 4 | 固化 CI 质量门禁 | 多编译器、Sanitizer | 新提交自动构建和测试 |
| 5 | 发布候选版本 | 安装、文档、变更日志 | 新用户独立运行成功 |

### 第 16 周：综合交付与独立考试

**周目标**：在 Codex 只 Review 的条件下独立交付一个版本。

| 课次 | 能力切片 | 验收产物 |
| --- | --- | --- |
| 1 | 拆分未见过的新需求 | API 草图、所有权图、验收与风险清单 |
| 2 | 实现最短纵向链路 | 可运行版本和首个测试 |
| 3 | 完成异常、边界和并发验证 | 测试与 Sanitizer 证据 |
| 4 | 真实负载和性能复核 | 与基线对比的报告 |
| 5 | 发布 v1.0 并答辩 | 安装包、文档、演示和复盘 |

## 8. Rust 与 C++ 对照学习

每两周选择一个已完成模块，填写对照表：

| 维度 | Rust 实现 | C++ 实现 | 证据与结论 |
| --- | --- | --- | --- |
| 所有权如何表达 |  |  |  |
| 资源何时释放 |  |  |  |
| 借用或引用如何保证有效 |  |  |  |
| 错误如何传播 |  |  |  |
| 并发安全由谁保证 |  |  |  |
| 静态与动态抽象成本 |  |  |  |
| 工具能发现哪些问题 |  |  |  |
| 最容易犯的错误 |  |  |  |

优先对照以下模块：文件资源、过滤器接口、LRU、任务队列和 HTTP 查询服务。

## 9. 作业、复习与源码阅读

### 9.1 每日作业

- 提交一个小而完整的 Git commit。
- 记录本次新增或转移的资源所有权。
- 保存一次编译、测试或 Sanitizer 证据。
- 安排一个隔日复现任务。

功能型提交消息示例：

```text
feat: 增加日志文件并行扫描能力
```

### 9.2 每周复习

- 无 AI 重写一个资源类型、算法或同步组件。
- 做一个业务不同的迁移题。
- 阅读一个工业模块，标注入口、对象关系、错误边界和线程假设。
- 运行一次非 Debug 配置，避免只在单一构建模式下验证。

### 9.3 间隔复习

- 1 天后：复现核心类型定义和资源边界。
- 1 周后：完成包含错误路径的变体题。
- 4 周后：把知识迁移到新资源或并发组件。
- 8 周后：在综合需求中重新论证接口和性能选择。

## 10. 评价与阶段验收

### 10.1 评分维度

| 维度 | 权重 | 优秀证据 |
| --- | ---: | --- |
| 功能正确性 | 20% | 正常、边界、失败行为明确 |
| 资源与生命周期安全 | 25% | RAII 清晰，零泄漏、悬空和双重释放 |
| 测试与动态验证 | 20% | 测试和对应 Sanitizer 全通过 |
| 接口与抽象设计 | 15% | 多态、值语义和泛型选择有依据 |
| 并发与性能证据 | 10% | 结论由 TSan、基准或 Profile 支持 |
| 独立完成度 | 10% | 在低 AI 参与下完成迁移需求 |

总分低于 70 不进入下一阶段。出现未解释的 UB、资源泄漏或数据竞争时，阶段直接判为未通过并安排补修。

### 10.2 每两周验收

1. 演示一个真实可用版本。
2. 展示测试、警告和 Sanitizer 结果。
3. 解释三个对象或资源的生命周期。
4. 完成一个未见过的迁移任务。
5. 更新一次 Rust/C++ 对照表。

## 11. Codex 协作协议

帮助按四级逐步升级：

1. **只提问**：询问所有者、有效期、失败路径和线程假设。
2. **只给方向**：指出问题属于生命周期、值语义、模板、同步或构建。
3. **最小独立示例**：给不超过 15 行、与 LogLens 无关的示例。
4. **代码审查**：检查正确性、资源、UB、异常安全、并发、性能和测试。

学习者独立尝试 10～15 分钟并保存编译器或测试证据后，才能升级帮助等级。任何 AI 生成代码都必须逐行解释，并通过警告、测试和相应 Sanitizer。

## 12. 完成定义

- [ ] Debug 和规定的 Release 构建均成功。
- [ ] 至少一个正常测试和一个边界或失败测试。
- [ ] 编译器警告清零。
- [ ] 当前阶段要求的 Sanitizer 全部通过。
- [ ] 使用真实输入完成一次运行。
- [ ] 能解释所有权、引用有效期和异常路径。
- [ ] 有性能结论时附带基准或 Profile。
- [ ] 形成一个小而完整的 Git commit。
- [ ] 安排下一次无 AI 复现任务。

## 13. 动态调整规则

### 任务过难

30 分钟仍无可运行结果时，缩减为一种资源、一个成功路径和一个失败路径。若生命周期尚不能解释，不增加模板或并发复杂度。

### 任务过易

增加异常注入、资源上限、不同编译器、并发时序或数据规模，不以使用更复杂语法作为升级难度的手段。

### 安全问题反复出现

若连续两周出现相同的悬空、越界或资源泄漏，暂停主项目一周，集中做最小生命周期实验和 Sanitizer 诊断。

### AI 依赖过高

若 Codex 连续两周生成主要实现，暂停新增功能，改为闭卷复现、错误定位和小型迁移题。Codex 仅允许提问或 Review。

### 进度落后

优先保留 RAII、值语义、测试、Sanitizer、并发正确性和综合交付。可压缩高级 PMR 实验、类型擦除变体或协程扩展，但不跳过安全验收。

## 14. 学习记录模板

```markdown
# 学习记录：<能力名称>

日期：
构建配置：
编译器：
Codex 帮助等级：1 / 2 / 3 / 4

## 本次目标与验收示例

## 资源与生命周期
- 所有者：
- 借用者：
- 释放时机：
- 失败路径：

## 验收证据
- 构建与警告：
- 自动测试：
- Sanitizer：
- 真实使用：

## 一个替代设计及其成本

## 当前实现的不足

## 下次无 AI 复现任务
```

## 15. 结课交付清单

- LogLens C++ Edition v1.0 源码与清晰的提交历史。
- Debug、Sanitizer、Release 和 CI 构建配置。
- 单元、集成、故障、并发和性能验证证据。
- 安装、配置、使用、排障和架构文档。
- 一份对象生命周期与并发不变量说明。
- 一份性能基线和证据驱动优化报告。
- 一份 Rust/C++ 关键模块对照报告。
- 一次不依赖 AI 生成实现的现场功能扩展与答辩。
